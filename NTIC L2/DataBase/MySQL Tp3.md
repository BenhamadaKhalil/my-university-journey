| Question No. | Query Description                                                                                                                                           | SQL Query                                                                                                                                                                                                                                                              |
| ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1            | List the pilots with more than 10000 flight hours.                                                                                                          | ```sql SELECT Nom, Prenom FROM Pilote WHERE NB_Heures > 10000; ```                                                                                                                                                                                                     |
| 2            | List the aircraft whose model starts with "B" and ends with "X".                                                                                            | ```sql SELECT * FROM Avion WHERE Modele LIKE 'B%' AND Modele LIKE '%X'; ```                                                                                                                                                                                            |
| 3            | How many aircraft have flown for more than 1000 hours?                                                                                                      | ```sql SELECT COUNT(DISTINCT Code) FROM Avion WHERE NB_Heures > 1000; ```                                                                                                                                                                                              |
| 4            | Display the total flight hours for each aircraft model.                                                                                                     | ```sql SELECT Modele, SUM(NB_Heures) AS Total_Flight_Hours FROM Avion GROUP BY Modele; ```                                                                                                                                                                             |
| 5            | Show the number of flights operated by each aircraft.                                                                                                       | ```sql SELECT Cd_Avion, COUNT(*) AS Number_Of_Flights FROM Vols GROUP BY Cd_Avion; ```                                                                                                                                                                                 |
| 6            | Which aircraft (Code) have been used for more than 2 flights?                                                                                               | ```sql SELECT Cd_Avion FROM Vols GROUP BY Cd_Avion HAVING COUNT(*) > 2; ```                                                                                                                                                                                            |
| 7            | Show the number of passengers who traveled on each flight.                                                                                                  | ```sql SELECT N_Vol, COUNT(*) AS Number_Of_Passengers FROM Passager GROUP BY N_Vol; ```                                                                                                                                                                                |
| 8            | Display the number of aircraft per brand.                                                                                                                   | ```sql SELECT Marque, COUNT(*) AS Number_Of_Aircraft FROM Avion GROUP BY Marque; ```                                                                                                                                                                                   |
| 9            | List the pilots (first and last names) who have flown to "Alger".                                                                                           | ```sql SELECT DISTINCT p.Nom, p.Prenom FROM Pilote p JOIN Vols v ON p.Matricule = v.Mt_Pilote WHERE v.Destination = 'Alger'; ```                                                                                                                                       |
| 10           | Which aircraft (Code, brand, and model) have been used for more than 2 flights?                                                                             | ```sql SELECT a.Code, a.Marque, a.Modele FROM Avion a JOIN Vols v ON a.Code = v.Cd_Avion GROUP BY a.Code, a.Marque, a.Modele HAVING COUNT(*) > 2; ```                                                                                                                  |
| 11           | Which aircraft models were used by pilots with more than 11000 flight hours? (Do not display duplicate rows)                                                | ```sql SELECT DISTINCT a.Modele FROM Avion a JOIN Vols v ON a.Code = v.Cd_Avion JOIN Pilote p ON v.Mt_Pilote = p.Matricule WHERE p.NB_Heures > 11000; ```                                                                                                              |
| 12           | Which pilots have flown both ATR and FOKER aircraft?                                                                                                        | ```sql SELECT p.Nom, p.Prenom FROM Pilote p JOIN Vols v ON p.Matricule = v.Mt_Pilote JOIN Avion a ON v.Cd_Avion = a.Code WHERE a.Modele IN ('ATR', 'FOKER') GROUP BY p.Matricule HAVING COUNT(DISTINCT a.Modele) = 2; ```                                              |
| 13           | Which aircraft models have transported more than 50 passengers in total? (Sorted in descending order)                                                       | ```sql SELECT a.Modele, SUM(p.NB_Passagers) AS Total_Passengers FROM Avion a JOIN Vols v ON a.Code = v.Cd_Avion JOIN Passager p ON v.Num_Vol = p.N_Vol GROUP BY a.Modele HAVING SUM(p.NB_Passagers) > 50 ORDER BY Total_Passengers DESC; ```                           |
| 14           | Which pilots have flown at least 3 different aircraft models?                                                                                               | ```sql SELECT p.Nom, p.Prenom FROM Pilote p JOIN Vols v ON p.Matricule = v.Mt_Pilote JOIN Avion a ON v.Cd_Avion = a.Code GROUP BY p.Matricule HAVING COUNT(DISTINCT a.Modele) >= 3; ```                                                                                |
| 15           | List the aircraft that have never been used for a flight.                                                                                                   | ```sql SELECT Code, Marque, Modele FROM Avion WHERE Code NOT IN (SELECT DISTINCT Cd_Avion FROM Vols); ```                                                                                                                                                              |
| 16           | Which aircraft has accumulated the most flight hours?                                                                                                       | ```sql SELECT Code, Modele, SUM(NB_Heures) AS Total_Flight_Hours FROM Avion JOIN Vols v ON Avion.Code = v.Cd_Avion GROUP BY Code ORDER BY Total_Flight_Hours DESC LIMIT 1; ```                                                                                         |
| 17           | Display the flight that transported the highest number of passengers.                                                                                       | ```sql SELECT Num_Vol, SUM(NB_Passagers) AS Total_Passengers FROM Passager GROUP BY Num_Vol ORDER BY Total_Passengers DESC LIMIT 1; ```                                                                                                                                |
| 18           | Show the total number of passengers transported by each pilot, sorted in descending order. Only display pilots who have transported at least 50 passengers. | ```sql SELECT p.Nom, p.Prenom, SUM(pas.NB_Passagers) AS Total_Passengers FROM Pilote p JOIN Vols v ON p.Matricule = v.Mt_Pilote JOIN Passager pas ON v.Num_Vol = pas.N_Vol GROUP BY p.Matricule HAVING SUM(pas.NB_Passagers) >= 50 ORDER BY Total_Passengers DESC; ``` |
| 19           | Display the number of pilots for each aircraft brand they have flown.                                                                                       | ```sql SELECT a.Marque, COUNT(DISTINCT v.Mt_Pilote) AS Number_Of_Pilots FROM Avion a JOIN Vols v ON a.Code = v.Cd_Avion GROUP BY a.Marque; ```                                                                                                                         |
| 20           | Show the aircraft (Code, model, and brand) that have never been used for a flight carrying more than 50 passengers.                                         | ```sql SELECT a.Code, a.Modele, a.Marque FROM Avion a WHERE a.Code NOT IN (SELECT DISTINCT v.Cd_Avion FROM Vols v JOIN Passager p ON v.Num_Vol = p.N_Vol GROUP BY v.Cd_Avion HAVING SUM(p.NB_Passagers) > 50); ```                                                     |
| 21           | Which pilots have flown to Istanbul, Alger, or Constantine between January 1, 2000, and January 31, 2020?                                                   | ```sql SELECT DISTINCT p.Nom, p.Prenom FROM Pilote p JOIN Vols v ON p.Matricule = v.Mt_Pilote WHERE v.Destination IN ('Istanbul', 'Alger', 'Constantine') AND v.Date_Vol BETWEEN '2000-01-01' AND '2020-01-31'; ```                                                    |

### Additional Questions:

| Question No. | Query Description | SQL Query |
|--------------|-------------------|-----------|
| 1 | Display the model and manufacturing year of aircraft built between 2018 and 2020. | ```sql SELECT Modele, Date_Fab FROM Avion WHERE Date_Fab BETWEEN '2018-01-01' AND '2020-12-31'; ``` |
| 2 | Display the total number of pilots recorded in the database. | ```sql SELECT COUNT(*) AS Total_Pilots FROM Pilote; ``` |
| 3 | List the first and last names of passengers who traveled in first class, sorted alphabetically. | ```sql SELECT Nom, Prenom FROM Passager WHERE Classe = 'First' ORDER BY Nom, Prenom; ``` |
| 4 | List the passengers (first name, last name, aircraft brand) who traveled on a flight operated by an "FOKER" aircraft in first class. | ```sql SELECT p.Prenom, p.Nom, a.Marque FROM Passager p JOIN Vols v ON p.N_Vol = v.Num_Vol JOIN Avion a ON v.Cd_Avion = a.Code WHERE a.Marque = 'FOKER' AND p.Classe = 'First'; ``` |
| 5 | Display the total number of flights completed by each pilot whose last name starts with the letter 'L', sorted in descending order. | ```sql SELECT p.Nom, COUNT(*) AS Total_Flights FROM Pilote p JOIN Vols v ON p.Matricule = v.Mt_Pilote WHERE p.Nom LIKE 'L%' GROUP BY p.Matricule ORDER BY Total_Flights DESC; ``` |
| 6 | Display the list of pilots who have flown to at least three different destinations. | ```sql SELECT p.Nom, p.Prenom FROM Pilote p JOIN Vols v ON p.Matricule = v.Mt_Pilote GROUP BY p.Matricule HAVING COUNT(DISTINCT v.Destination) >= 3; ``` |

---

---
## 🔗 Navigation
- **Module:** [[DataBase|◀ DataBase]]
- **Semester:** [[NTIC L2|◀ NTIC L2]]
- **Academic Home:** [[README|🏠 Home]]
